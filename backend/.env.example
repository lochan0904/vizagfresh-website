# Server
PORT=4000
NODE_ENV=development
CLIENT_ORIGIN=http://localhost:5173

# Database (either DATABASE_URL, or the individual PG* vars below)
DATABASE_URL=postgres://vizagfresh:vizagfresh@localhost:5432/vizagfresh
# PGHOST=localhost
# PGPORT=5432
# PGUSER=vizagfresh
# PGPASSWORD=vizagfresh
# PGDATABASE=vizagfresh

# Auth
JWT_SECRET=change-this-to-a-long-random-string
JWT_EXPIRES_IN=12h

# Seeded admin account (used by db/seeds/02_admin.js)
ADMIN_EMAIL=admin@vizagfresh.in
ADMIN_PASSWORD=ChangeMe123!

# WhatsApp business number used by the frontend order handoff (E.164, no +)
WHATSAPP_NUMBER=919999999999
